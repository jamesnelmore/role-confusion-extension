[project]
name = "role-confusion-eval"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "inspect-ai>=0.3",
    "openai>=3.7.0",
    "pyyaml",
]

[project.optional-dependencies]
steering = [
    "torch",
    "transformers>=4.56",
    "accelerate",
    "datasets",
]
